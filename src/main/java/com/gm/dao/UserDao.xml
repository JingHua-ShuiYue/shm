<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gm.dao.UserDao">
    <!--登录验证-->
    <select id="selectUser" resultType="com.gm.pojo.User" parameterType="com.gm.pojo.User">
        select * from user where username=#{username} and password=#{password}
    </select>
    <insert id="insertStudent" parameterType="com.gm.pojo.User">
        insert into user(username, password, role) values(#{username}, #{password}, 'student')
    </insert>
    <insert id="insertTeacher" parameterType="com.gm.pojo.User">
        insert into user(username, password, role) values(#{username}, #{password}, 'teacher')
    </insert>
    <delete id="delUser">
        delete from User where id=#{id} and #{role}='admin'
    </delete>
    <select id="selectUsers" resultType="com.gm.pojo.User">
        select * from User where role=#{role}
    </select>
    <select id="selectUserByUsername" resultType="com.gm.pojo.User">
        select * from user where username=#{username}
    </select>
    <select id="getStudentInfoByUser" resultType="com.gm.pojo.Student">
        select * from user where username=
    </select>


</mapper>